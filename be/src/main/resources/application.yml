spring:
  application:
    name: be

  profiles:
    active: jwt, local

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: trace

  #  # security logging
  #  org.springframework.security: DEBUG